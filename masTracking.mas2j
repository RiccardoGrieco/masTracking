MAS masTracking {
    infrastructure: Centralised
    environment: HouseEnv(gui,level.txt,rooms.json) // use "nogui" as parameter to not show the GUI
    agents:
        camera;
    aslSourcePath:
        "src/asl";
}


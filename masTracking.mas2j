MAS masTracking {
    infrastructure: Centralised
    environment: HouseEnv(gui,level.txt) // use "nogui" as parameter to not show the GUI
    agents:
        camera;
    aslSourcePath:
        "src/asl";
}


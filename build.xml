<?xml version="1.0" encoding="ISO-8859-1" ?>

<!--

                    This file was generated by Jason null
                    http://jason.sf.net

                    luglio 09, 2018 - 18:55:32
-->

<project name   ="masTracking"
         basedir="."
         default="run">

    <property name="mas2j.project.file" value="masTracking.mas2j"/>
    <property name="debug" value=""/> <!-- use "-debug" to run in debug mode -->
    <property name="build.dir" value="${basedir}/target/classes" />
    <property name="library.dir" value="C:\Users\marco\.m2\repository"/>
    <property name="resource.dir" value="${basedir}\src\main\resources"/>

    <!--Dipendenze prese con maven -->
    <property name="jasonJar" value="${library.dir}\net\sf\jason\jason\2.2\jason-2.2.jar"/>
    <property name="jsonJar" value="${library.dir}\org\json\json\20180130\json-20180130.jar"/>
    <property name="jgraphtJar" value="${library.dir}\org\jgrapht\jgrapht-core\1.2.0\jgrapht-core-1.2.0.jar"/>
    <property name="jasonJar" value="${library.dir}\net\sf\jason\jason\2.2\jason-2.2.jar"/>


    <path id="project.classpath">
        <pathelement location="${basedir}"/>
        <pathelement location="${build.dir}"/>
        <pathelement location="${jasonJar}"/>
        <pathelement location="${jsonJar}"/>
        <pathelement location="${jgraphtJar}"/>
        <pathelement location="${exec.dir}"/>

    </path>

    <!-- tasks the user can override in his/her c-build.xml script -->
    <target name="user-init">
    </target>

    <target name="user-end">
    </target>

    <target name="init">
        <mkdir dir="${build.dir}" />
        <antcall target="user-init" />
    </target>

       <target name="compile" depends="init">
        <condition property="srcdir" value="${basedir}/src/java" else="${basedir}" >
            <available file="${basedir}/src/java" />
        </condition>
        <javac srcdir="${srcdir}" destdir="${build.dir}" debug="true" optimize="true" includeantruntime="false" >
            <classpath refid="project.classpath"/>
        </javac>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${basedir}/lib" />
        <delete file="${ant.project.name}.jar" />
        <copy file="${jasonJar}" tofile="${ant.project.name}.jar" />
        <copy file="${mas2j.project.file}" tofile="default.mas2j" />
        <jar update="yes" jarfile="${ant.project.name}.jar" >
            <fileset dir="${basedir}">
                <include name="**/*.asl" />
                <include name="**/*.mas2j" />
            </fileset>
            <fileset dir="${build.dir}">
                <include name="**/*.class" />
            </fileset>
            <zipgroupfileset dir="${basedir}/lib" includes="**/*.jar" />
            <zipgroupfileset dir="${jasonJar}/../../libs" includes="*.jar" />
            <manifest>
                  <attribute name="Main-Class" value="jason.infra.centralised.RunCentralisedMAS"/>
            </manifest>
        </jar>
        <delete file="default.mas2j" />
    </target>

    <target name="run" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jason.infra.centralised.RunCentralisedMAS"
              failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="${mas2j.project.file} ${debug} "/>
            <!-- jvmarg line="-Xmx500M -Xss8M"/ -->
        </java>
        <antcall target="user-end" />
    </target>

    <target name="clean" >
        <delete failonerror="no" includeEmptyDirs="true" verbose="true">
            <fileset dir="${basedir}" includes="**/*.class"/>
        </delete>
    </target>



</project>
